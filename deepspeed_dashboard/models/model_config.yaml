# 模型配置文件
model_name: "EleutherAI/gpt-neox-20b"  # 默认模型
device_map: "auto"                     # 默认设备映射策略

# 默认生成参数
default_generation:
  temperature: 0.7
  top_k: 50
  top_p: 0.9
  max_new_tokens: 256
  repetition_penalty: 1.0
